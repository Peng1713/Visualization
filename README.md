# 数据可视化大屏平台

基于 Next.js 14 + React + TypeScript + Ant Design + ECharts 构建的现代化数据可视化大屏系统。

## 技术栈

- **框架**: Next.js 14 (App Router)
- **语言**: TypeScript
- **UI 组件**: Ant Design 5
- **图表**: ECharts + echarts-for-react
- **样式**: Emotion + Tailwind CSS
- **状态管理**: Zustand

## 功能特性

- ✨ **炫酷登录界面** - 粒子动画背景、毛玻璃卡片、渐变主题
- 📊 **可视化大屏** - 销售趋势、品类分布、区域对比、能力雷达等多维度图表
- 🎨 **主题切换** - 支持黑色/白色主题
- 📐 **导航布局** - 左侧/顶部导航可切换
- 📱 **响应式设计** - 完美兼容手机、平板、PC
- 🔐 **权限预留** - 路由配置支持权限码、角色控制，可对接接口动态配置

## 快速开始

```bash
# 安装依赖
npm install

# 开发模式
npm run dev

# 生产构建
npm run build

# 启动生产服务
npm start
```

## 项目结构

```
src/
├── app/                    # Next.js App Router 页面
│   ├── page.tsx           # 登录页（根路径）
│   ├── dashboard/         # 主应用（需登录）
│   │   ├── page.tsx       # 数据大屏
│   │   ├── analytics/     # 数据分析
│   │   ├── reports/       # 报表中心
│   │   └── settings/     # 系统设置
│   └── layout.tsx
├── components/
│   ├── LoginPage/         # 登录组件
│   ├── MainLayout/        # 主布局（导航、主题）
│   └── Dashboard/         # 大屏图表组件
├── store/                 # Zustand 状态
├── types/                 # 类型定义
│   └── route.ts          # 路由配置（含权限预留）
└── utils/
    └── routeGuard.ts     # 路由权限守卫
```

## 登录说明

开发环境可使用任意用户名和密码登录，用于演示。

## 权限扩展

路由权限配置位于 `src/types/route.ts`，支持：

- `permission`: 权限码数组，可对接后端接口
- `roles`: 角色数组
- `hidden`: 是否隐藏菜单

权限校验逻辑在 `src/utils/routeGuard.ts`，可根据实际需求扩展。

## 浏览器支持

现代浏览器（Chrome、Firefox、Safari、Edge）及移动端。
